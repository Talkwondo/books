import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  contianer: {
    justifyContent: 'flex-start',
    paddingTop: 30,
  },
  h1: {
    fontSize: 30,
    textAlign: 'left',
    marginVertical: 30,
    paddingLeft: 50,
    color: 'white',
  },
});
